FLASK_APP=run.py
FLASK_ENV=development
SECRET_KEY=replace-with-a-strong-secret
DATABASE_URL=sqlite:///mediguide.db

# Google API (optional) - if empty app uses seeded hospital data
GOOGLE_API_KEY=
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# Email settings for reminders (dev: just logs)
MAIL_SERVER=localhost
MAIL_PORT=25
MAIL_USERNAME=
MAIL_PASSWORD=
